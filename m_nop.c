#include "monty.h"
/**
 * m_nop - a function that does nothing
 *
 * @stack: double pointer to the first element in the stack
 * @line_number: line number in the script
 *
 */
void m_nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
